import { Box, Typography } from '@mui/material';
import { FC } from 'react';

export const HomePage: FC = () => {
  return (
    <Box display={'flex'} justifyContent={'center'} mt={5}>
      <Typography variant="h4">Welcome to AIAB Admin Portal1</Typography>
    </Box>
  );
};
